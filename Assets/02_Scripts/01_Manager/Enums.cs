public enum GameState
{
    GAME_READY,
    GAME_START,
    GAME_CLAER,
    GAME_OVER,
}

public enum GameLevel
{
    NULL,
    EASY = 10,
    NORMAL,
    HARD
}
public enum SceneNumber
{
    MAIN_SCENE,
    START_SCENE,
}